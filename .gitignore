# Dart / Flutter
*.png
.dart_tool/
.packages
build/
pubspec.lock
!/pubspec.lock
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
*.iml
.vscode/

# macOS
.DS_Store

# build symlinks (relocated to another volume) — "build/" only matches real dirs
build

# Claude Code agent worktrees (isolated, not tracked)
.claude/worktrees/

# Claude Code local working notes (not tracked)
CLAUDE.md

# covfuzz artifacts
.corpus/
.crashes/
